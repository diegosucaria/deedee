{
    "homeassistant": {
        "transport": "stdio",
        "command": "uvx",
        "args": [
            "mcp-server-home-assistant"
        ],
        "env": {
            "HA_URL": "${HA_URL}",
            "HA_TOKEN": "${HA_TOKEN}"
        },
        "disabled": false
    }
}