{
    "homeassistant": {
        "transport": "stdio",
        "command": "uvx",
        "args": [
            "ha-mcp"
        ],
        "env": {
            "HA_URL": "${HA_URL}",
            "HA_TOKEN": "${HA_TOKEN}"
        },
        "disabled": false
    }
}