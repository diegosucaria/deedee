{
    "homeassistant": {
        "transport": "stdio",
        "command": "uvx",
        "args": [
            "mcp-homeassistant"
        ],
        "env": {
            "HA_URL": "${HA_URL}",
            "HA_TOKEN": "${HA_TOKEN}"
        },
        "disabled": false
    }
}