{
    "homeassistant": {
        "transport": "sse",
        "url": "${HA_URL}/api/mcp",
        "env": {
            "HA_URL": "${HA_URL}",
            "HA_TOKEN": "${HA_TOKEN}"
        },
        "disabled": false
    }
}