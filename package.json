{
  "name": "deedee-monorepo",
  "version": "0.0.1",
  "packageManager": "npm@10.8.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "NODE_OPTIONS=--experimental-vm-modules jest apps/web --passWithNoTests",
    "lint": "eslint .",
    "build": "npm run build --workspace=apps/web",
    "prepare": "husky"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "husky": "^9.1.7",
    "jest": "^29.7.0",
    "typescript": "^5.3.3"
  }
}
